# Optional — used by sync-apk-catalog for GitHub API calls and rate limits.
# Required for make-repos-public (org owner / repo admin classic PAT with `repo` scope).
# https://github.com/settings/tokens
GITHUB_TOKEN=
